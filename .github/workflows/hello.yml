name: Hello # ワークフロー名
on: push # トリガーイベント
jobs: # ジョブの定義
  hello: # ジョブ名
    runs-on: ubuntu-latest # 実行環境
    steps:
      - run: echo "Hello, World!" # 実行内容
      - uses: actions/checkout@v4 # アクションの呼び出し
