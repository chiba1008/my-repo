name: Manual
on:
  workflow_dispatch:
    inputs:
      greetubg:
        type: string
        default: "Hello"
        required: true
        description: "Greeting message"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.inputs.greeting }}, World!"
